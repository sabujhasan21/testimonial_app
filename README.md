# Streamlit Testimonial Generator

This app generates official testimonial PDFs from Excel student data.

## Run locally
```bash
pip install -r requirements.txt
streamlit run streamlit_testimonial.py
```

## Deploy
Push this repo to GitHub and use Streamlit Cloud to deploy.
